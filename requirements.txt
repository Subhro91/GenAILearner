# Web Framework and Server
fastapi==0.115.11        # Modern, fast web framework for building APIs
uvicorn==0.34.0          # ASGI server for FastAPI
python-multipart==0.0.20 # For handling form data and file uploads
jinja2==3.1.6            # Template engine for HTML rendering

# PDF Processing
PyMuPDF==1.25.3          # PDF parsing and text extraction

# Machine Learning and AI
numpy<2.0.0              # NumPy 1.x for compatibility with PyTorch
transformers==4.49.0     # Hugging Face Transformers for NLP models
torch==2.2.1             # PyTorch for deep learning (CPU or GPU version)

# Utilities
python-dotenv==1.0.1     # For loading environment variables
psutil==5.9.8            # For system and process utilities 